TÓM TẮT 5 CHƯƠNG ĐẦU
Chương 1 – Giới thiệu Full Stack

Full Stack = làm cả Front-end + Back-end + Database.

Công nghệ front-end: HTML, CSS, JS, jQuery, Bootstrap.

Back-end: Java + Spring MVC/Spring Boot.

REST API, JSON, ORM (Hibernate).
→ Hiểu bức tranh tổng thể của phát triển web.

Chương 2 – Phân tích dự án E-Commerce

Hiểu ERD, Page Flow, các entity: Customer, Product, Order, Payment…

Xác định các trang cần làm theo luồng mua hàng.

Thiết kế API (GET/POST).
→ Học cách tư duy hệ thống trước khi bắt đầu code.

Chương 3 – HTML cơ bản

Cấu trúc HTML: html/head/body.

Thẻ: heading, paragraph, links, tables, images,…

Thuộc tính & sự kiện (onclick, onchange…).
→ Xây dựng được trang web tĩnh đơn giản.

Chương 4 – CSS cơ bản

Cú pháp CSS, selectors (id, class, element).

Màu sắc, background, box model, margin/padding.

Ba cách nhúng CSS: inline, internal, external.
→ Biết cách làm giao diện đẹp, gọn, dễ quản lý.

Chương 5 – jQuery

Cú pháp jQuery: $(selector).action().

Selectors, events (click, mouseenter…), animations.

Effects: hide/show, fade, slide, toggle.

DOM manipulation, chaining, callback.
→ Thêm tương tác động vào webpage bằng JS dễ dàng hơn.
